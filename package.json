{
  "devDependencies": {
    "vitepress": "^1.1.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev docs --host --port 5200",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  }
}
