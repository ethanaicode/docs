# NodeJs

> npm 用于包管理
>
> npx 用于简单命令（临时用）

> npm update
>
> npm start 启动本地服务

### npm set

```js
npm set registry https://registry.npmjs.org/
```

### npm install

当你下载的包报错后，删除`node_modules`文件夹，再重新使用`npm install`试试。

### npx run

本地开发：npm run dev
